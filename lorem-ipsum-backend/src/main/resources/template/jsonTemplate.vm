## @formatter:off
{
    "data": {
#foreach( $row in $rowList )
        "entry": {
#foreach( $entry in $row.getEntryList())
            "${entry.getName()}": "${entry.getData()}"#if($foreach.hasNext),#end

#end
        }#if($foreach.hasNext),#end

#end
    }
}
## @formatter:on